This test abstract validates the foreign-language pre-textual element, its heading and the keyword block in the UFC V2 class. The content is intentionally short because this fixture checks document structure and formatting rather than the semantic word-count requirement of the final work.

\keywords{LaTeX; normalization; UFC.}
