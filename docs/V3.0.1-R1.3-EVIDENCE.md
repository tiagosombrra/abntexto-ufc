# v3.0.1 R1.3 evidence — coherent canonical TCC rewrite

Tracking issue: #304  
PR: #305  
Work package: R1  
Sub-lot: R1.3  
Status: IMPLEMENTED_PENDING_STATIC_RERUN

## Rewrite policy

R1.3 is an additive, bounded rewrite against the frozen R1.2 findings. The seven chapter bodies were proven to survive from the reviewed commented-guide commit up to the R1.3 boundary, so the implementation does not replace the guide with a new fictitious document.

No class runtime, normative rule value, source authority, profile behavior or release threshold is intentionally changed by this lot. The goal is to make the existing canonical TCC a complete user-facing explanation of how major requirements map to project mechanisms and evidence.

## Changed canonical source files

- `template/main.tex`
- `template/chapters/1-introduction.tex`
- `template/chapters/2-academic-work-structure.tex`
- `template/chapters/3-front-matter.tex`
- `template/chapters/4-general-formatting.tex`
- `template/chapters/5-citations-notes-references.tex`
- `template/chapters/7-abntexto-ufc-resources-and-final-review.tex`

`template/chapters/6-academic-objects.tex` is deliberately retained unchanged in this lot because its reviewed visual/object corpus is already strong. R1.3 adds configuration/evidence explanation around that corpus instead of rewriting it.

## New teaching vocabulary

`template/main.tex` retains the existing four authority/source callouts and adds two local documentation-only callouts:

- `\guiamecanismo{...}` — identifies the public key, command, environment, file or workflow a user actually manipulates;
- `\guiavalidacao{...}` — identifies the gate/evidence route and its boundary.

These commands are local to the reference document and do not change the public class API.

## R1.2 gap closure map

| R1.2 finding | R1.3 action |
|---|---|
| `ufcsetup` configuration guide partial | Chapter 1 groups profile/output, institutional/academic, identity and advisor/examiner metadata and explains the single-source-of-truth policy. |
| authority versus implementation/evidence not consistently explicit | Chapters use separate authority, mechanism and validation callouts. |
| optional lists and populated unified illustrations partial | Chapter 3 explains each list API and states that a populated illustration list is required to prove entry write/render behavior. |
| pagination/duplex duplicated | Chapter 2 is reduced to structural overview; Chapter 4 owns geometry, visible numbering and duplex validation. |
| font identity versus portability partial | Chapter 4 separates requested family, strict literal identity and portable fallback. |
| font embedding missing | Chapter 4 adds a dedicated section distinguishing embedding from font-family identity and ties it to generated-PDF release checks. |
| footnotes partial | Chapter 4 contains a real rendered `\footnote` example plus mechanism/evidence guidance. |
| citation variants partial | Chapter 5 explains `\cite`, `\textcite`, locator usage, long/short direct quotation boundaries, `\apud`, `\textapud` and their dedicated evidence routes without inventing locators for synthetic text. |
| code/algorithm policy boundary partial | Chapter 7 cross-references the retained object corpus and separates module/editorial choices from ABNT requirements; it names the relevant gates. |
| supported profiles incomplete in prose | Chapter 7 lists all seven certified profiles, including `scientific-article`, and identifies the article as a separate source example rather than a TCC chapter variant. |
| build workflow partial | Chapter 7 explains `make compile`, conditional Biber/glossary/index processing, `make check` and the stronger release gate. |
| CLI/Deep user workflow missing | Chapter 7 adds a dedicated CLI/Deep section tied to `tools/validate-ufc-pdf.py` and the `pdf-validator` suite. |
| Web/Lite user workflow missing | Chapter 7 adds a bounded Web/Lite section: local browser processing, shared-check semantics, Deep-only boundaries and E2E conditions required for release certification. It does not claim R4 is complete. |
| accessibility guidance missing | Chapter 7 adds a bounded accessibility section and rejects a false PDF/UA claim based only on PDF/A or selected structural checks. |
| final checklist incomplete | Final checklist includes profile/setup, complete build, PDF geometry/embedding, validator interpretation, visual review, manual accessibility review and exact-SHA release discipline. |

## Additional correction discovered during R1.3

The previous `Perfis de documento` prose named six profiles while release review/certification uses seven. The missing prose entry was `scientific-article`. This is corrected in the guide; no profile implementation changed.

## Preservation constraints retained

R1.3 preserves the existing material on current ABNT editions/UFC precedence, detailed pre-textual layout, long-quotation geometry, NBR 6023:2025 reference layout, the complete visual object corpus, IBGE tables, appendices/annexes/index, PDF/A versus deposit policy, and strict-font versus portable fallback. `template/chapters/6-academic-objects.tex` remains byte-identical to the R1.2 boundary.

## First R1.3 CI result — failed closed in governance documentation

Implementation commit: `f8452bc3507a5206fc4e5a4c837feef872f23f3c`.

- Linux Integration #398 (`34481185134`): completed successfully under draft/development scope; **not** final release evidence.
- Static Contract #478 (`34481185238`): **FAIL**.

The Static failure occurred before the TCC/reference contracts ran. `tests/checks/phase_governance.py` rejected `AGENTS.md` because the R1.2 handoff rewrite had replaced the exact required governance phrase `material advance` with `material lot`. The checker also requires the explicit phrase `phase-end regression`.

Classification: `CONTROL_DOCUMENTATION_CONTRACT_DRIFT`. This is not a reason to weaken the checker. `AGENTS.md` is corrected to restore both canonical concepts while retaining the new self-contained handoff hierarchy. The failed #478 run remains part of the audit trail.

## Required checks before closure

R1.3 is not DONE until the corrected head proves at least:

1. Static contract PASS, including phase governance and reference-guide contract;
2. canonical reference build/integration PASS;
3. no new unrecognized warning/overflow in the reference path;
4. any failure caused by the new prose is fixed without weakening existing contracts.

A draft/scoped Linux success is useful development evidence but is not R5 release evidence.
