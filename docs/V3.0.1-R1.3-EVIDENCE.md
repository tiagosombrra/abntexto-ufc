# v3.0.1 R1.3 evidence — coherent canonical TCC rewrite

> **Historical accepted/development evidence — not current release authority.** This file records an earlier v3.0.1 lot and remains part of the audit trail. Current phase/status is controlled by the v3.0.1 continuation/finalization files; see `docs/V3.0.1-DOCUMENT-LIFECYCLE.md`.


Tracking issue: #304  
PR: #305  
Work package: R1  
Sub-lot: R1.3  
Status: DONE
Closure source SHA: `704cedaa9960b87ae6035ac08fa4cc4c286ea9aa`

## Rewrite policy

R1.3 was an additive, bounded rewrite against the frozen R1.2 findings. The seven chapter bodies were proven to survive from the reviewed commented-guide commit up to the R1.3 boundary, so the implementation did not replace the guide with a new fictitious document.

No class runtime, normative rule value, source authority, profile behavior, margin threshold or release threshold was intentionally changed by this lot. The goal was to make the existing canonical TCC a complete user-facing explanation of how major requirements map to project mechanisms and evidence.

The reviewed object corpus in `template/chapters/6-academic-objects.tex` remained unchanged by R1.3.

## Teaching changes

R1.3 adds local documentation-only `\guiamecanismo{...}` and `\guiavalidacao{...}` callouts and closes the R1.2 guidance gaps: coherent `\ufcsetup` explanation; optional-list behavior; pagination/duplex consolidation; literal-font versus fallback and embedding guidance; a rendered footnote; citation variants/`apud`; all seven profiles including `scientific-article`; code/algorithm/equation policy boundaries; full build workflow; CLI/Deep; bounded Web/Lite guidance; accessibility/manual-review limits; and the final delivery checklist.

## Preserved audit trail

1. Static #478 (`34481185238`) on `f8452bc3507a5206fc4e5a4c837feef872f23f3c`: **FAIL** — `AGENTS.md` had lost the exact governance concept `material advance`. The checker was not weakened.
2. Commit `e2f1998ac9e565767d85aedbcd65f2fa48e38d0d` restored explicit `material advance` and `phase-end regression` wording.
3. Static #479 (`34481697858`): **PASS**.
4. Linux #399 (`34481697908`): workflow success but heavy integration skipped because the PR was draft; not acceptance evidence.
5. PR #305 was moved to ready-for-review solely so repository-owned Linux integration could run. This does not authorize merge.
6. Linux #400 (`34481808802`) on `e2f1998...`: **FAIL**, scope `complete`, `PASS=32 FAIL=1 SKIP=3`. The sole failure was `reference`.
7. Root cause of #400: the R1.3 subsection heading contained `\ufcsetup` inside a moving argument. When `main.toc` was re-read, LaTeX executed it and reported unknown key `ufc/\check@icr`.
8. The heading was corrected to the plain semantic title `Configuração central do documento`, retaining `\ufcsetup` only in body text.
9. Static #481 (`34484253436`) on `ba8aeb1cce86288652acac40899ae721445bc880`: **PASS**.
10. Linux #402 (`34484253425`) began a real integration run but selected a broader scope because the active machine-state JSON was treated as an unknown technical path. It was later **CANCELLED** by workflow concurrency when the successor commit started. It remains audit evidence, not acceptance evidence.
11. Commit `1438a7cad27116170440535b9f62e2c084c0c8b8` classified `release/v3.0.1-final-corrections.json` as documentation/control state while preserving fail-closed handling of unknown technical files and release candidate markers.
12. Static #482 (`34485632344`) on `1438a7c...`: **PASS**.
13. Linux #403 (`34485632570`) on `1438a7c...`: **FAIL**, scope `reference-document`, `PASS=0 FAIL=1 SKIP=2`. `make compile` generated a 63-page PDF, but the warning gate found one `Underfull \hbox` and one `Overfull \hbox` caused by oversized monospaced full command invocations in chapter 5.
14. Those invocations were split into smaller command-name, argument and path units without changing bibliography semantics or warning thresholds.
15. Commit `e7f6fc88618329505e7f0de4449aec7c54fe45b0` contains that typography correction plus synchronized machine state, handoff and evidence.
16. Static #483 (`34487258470`) on `e7f6fc8...`: **PASS**.
17. Linux #404 (`34487258455`) on `e7f6fc8...`: **FAIL**, scope `reference-document`, `PASS=2 FAIL=1 SKIP=0`. `reference` **PASS** and `reference-corpus` **PASS**. `pdf-validator` alone failed `layout.margins` because PDF.js reported one em dash on page 23 at `(82.6, 94.9)` pt, about 0.6 pt beyond the configured left-margin threshold including tolerance.
18. Source correlation identified that punctuation as the editorial em dash pair around `como \section e \subsection` in chapter 2. The sentence was rewritten with commas. Margin values, tolerance and validator logic were not relaxed.
19. Commit `704cedaa9960b87ae6035ac08fa4cc4c286ea9aa` contains the page-23 punctuation correction and synchronized control documentation.
20. Static #484 (`34488189988`) on `704ceda...`: **PASS**.
21. Linux #405 (`34488190044`) on `704ceda...`: **PASS**, scope `reference-document`, summary `PASS=3 FAIL=0 SKIP=0`. Individual results: `reference=PASS`, `reference-corpus=PASS`, `pdf-validator=PASS`.

## Bounded CI-scope correction discovered during R1.3

The active correction state is machine-readable documentation/control-plane state. Updating it alone does not force heavy integration, matching the treatment of `release/v3-roadmap.json` and `docs/**`. Unknown technical paths still select `complete`; runtime/core/standards changes retain required scopes; final/release candidate markers force `complete`; orchestration-only changes select `smoke`; canonical TCC chapter changes select `reference-document`.

`tests/checks/linux_integration_suites.py` and the selector self-test protect this classification.

## Closure decision

R1.3 is **DONE**. Its acceptance source is exactly `704cedaa9960b87ae6035ac08fa4cc4c286ea9aa`, with fresh Static #484 PASS and real Linux #405 `reference-document` PASS (`3/3`, no failures or skips).

The #478, #400, #403 and #404 failures remain permanent evidence of defects found and corrected. The green #405 does not erase them.

R2 may begin after this closure is committed to the repository control documents. R2 must reuse the existing deterministic release-reference path and must not treat R1.3 targeted evidence as the final R5 phase-end certification.
