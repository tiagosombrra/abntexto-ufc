# v3.0.1 R1.3 evidence — coherent canonical TCC rewrite

Tracking issue: #304  
PR: #305  
Work package: R1  
Sub-lot: R1.3  
Status: CORRECTION_PENDING_TARGETED_RERUN

## Rewrite policy

R1.3 is an additive, bounded rewrite against the frozen R1.2 findings. The seven chapter bodies were proven to survive from the reviewed commented-guide commit up to the R1.3 boundary, so the implementation does not replace the guide with a new fictitious document.

No class runtime, normative rule value, source authority, profile behavior or release threshold is intentionally changed by this lot. The goal is to make the existing canonical TCC a complete user-facing explanation of how major requirements map to project mechanisms and evidence.

The reviewed object corpus in `template/chapters/6-academic-objects.tex` remains unchanged by R1.3.

## Teaching changes

R1.3 adds local documentation-only `\guiamecanismo{...}` and `\guiavalidacao{...}` callouts and closes the R1.2 guidance gaps: coherent `\ufcsetup` explanation; optional-list behavior; pagination/duplex consolidation; literal-font versus fallback and embedding guidance; a rendered footnote; citation variants/`apud`; all seven profiles including `scientific-article`; code/algorithm/equation policy boundaries; full build workflow; CLI/Deep; bounded Web/Lite guidance; accessibility/manual-review limits; and the final delivery checklist.

## Preserved audit trail

1. Static #478 (`34481185238`) on `f8452bc3507a5206fc4e5a4c837feef872f23f3c`: **FAIL** — `AGENTS.md` had lost the exact governance concept `material advance`. The checker was not weakened.
2. Commit `e2f1998ac9e565767d85aedbcd65f2fa48e38d0d` restored explicit `material advance` and `phase-end regression` wording.
3. Static #479 (`34481697858`): **PASS**.
4. Linux #399 (`34481697908`): workflow success but heavy integration skipped because the PR was draft; not acceptance evidence.
5. PR #305 was moved to ready-for-review solely so repository-owned Linux integration could run. This does not authorize merge.
6. Linux #400 (`34481808802`) on `e2f1998...`: **FAIL**, scope `complete`, `PASS=32 FAIL=1 SKIP=3`. The sole failure was `reference`.
7. Root cause of #400: the R1.3 subsection heading contained the `\ufcsetup` configuration command inside a moving argument. When `main.toc` was re-read, LaTeX executed it and reported unknown key `ufc/\check@icr`.
8. The heading was corrected to the plain semantic title `Configuração central do documento`, retaining `\ufcsetup` only in body text.
9. Static #481 (`34484253436`) on `ba8aeb1cce86288652acac40899ae721445bc880`: **PASS**.
10. Linux #402 (`34484253425`) started a real integration run, but the incremental selector treated `release/v3.0.1-final-corrections.json` as an unknown technical file and therefore selected a broader scope than a state/handoff update should require. This run is intentionally superseded by the bounded selector correction below; its eventual remote conclusion remains part of the audit trail but is not required to close R1.3.

## Bounded CI-scope correction discovered during R1.3

The active correction state is machine-readable documentation/control-plane state. Updating it alone must not force heavy integration, just as `release/v3-roadmap.json` and `docs/**` do not. Otherwise the required rule “update documentation on every material advance” mechanically launches a full Linux regression for bookkeeping changes.

The selector is therefore corrected so `release/v3.0.1-final-corrections.json` is `docs-only`. Fail-closed behavior is preserved:

- unknown technical paths still select `complete`;
- runtime/core/standards changes still select their required scopes or `complete`;
- final/release candidate markers still force `complete`;
- orchestration-only changes still select `smoke`;
- the corrected TCC chapter remains a real `reference-document` trigger.

`tests/checks/linux_integration_suites.py` and the selector self-test explicitly protect this classification. The source also receives a short comment explaining why executable configuration commands must not return to the `.toc`-replayed heading.

## Closure condition

R1.3 becomes DONE only after the successor head obtains:

- fresh Static PASS; and
- real Linux integration in which the `reference` check passes (directly or through a superset scope).

The #478 and #400 failures remain permanent audit evidence. R2 must not begin before this closure condition is satisfied.
