# v3.0.1 R1.1 evidence — source identity normalization

Tracking issue: #304
PR: #305
Work package: R1
Sub-lot: R1.1
Status: DONE

## Implementation

The canonical TCC chapter paths were normalized without rewriting chapter bodies. Existing chapter blobs were reused under their new semantic paths, and `template/main.tex` was changed to load the seven numbered chapters in order.

## First Static result — failed closed

Static contract run #474 (`34476591229`) failed on head `54cde73b1c0c9c54cc752e6e3f95a752a0107a32`.

The failure was not a LaTeX/content regression. `tests/checks/reference_guide_contract.py` and `standards/reference-guide-map.json` still encoded the previous chapter paths. The contract therefore correctly failed closed when the files were renamed.

Observed stale references included:

- `template/chapters/3-methodology.tex`;
- `template/chapters/related-work.tex`;
- `template/chapters/4-results.tex`;
- `template/chapters/formatting-examples.tex`;
- `template/chapters/5-conclusion.tex`.

The librarian item 11 check also loaded `template/chapters/formatting-examples.tex` directly, so all five expected object titles appeared missing after the rename.

## Correction

The source-to-rule map was updated to the new canonical paths and the librarian checker now uses `template/chapters/6-academic-objects.tex`.

No normative rule IDs, source IDs, classifications, markers, object titles, section labels, thresholds or acceptance semantics were changed. The correction only followed the R1.1 path rename through the existing test/documentation dependency graph.

## Closing Static result

Static contract run #475 (`34476976677`) passed on head `5394d2caa3c12ec69c87c38c01302df73c9e59c9`.

Linux integration run #395 (`34476976597`) also completed successfully, but the PR remained draft and that run is not classified as final release evidence.

## R1.1 closure

R1.1 is closed because:

- `template/main.tex` loads exactly seven sequential chapter paths;
- six stale semantic filenames were removed;
- replacement chapter paths reuse the original chapter blobs, so chapter content was not silently rewritten during the rename;
- reference-guide map/checker consumers were reconciled;
- the fresh Static contract passed.

The failed #474 run remains part of the permanent audit trail rather than being hidden by the successful rerun.
