# v3.0.1 R1.1 evidence — source identity normalization

Tracking issue: #304
PR: #305
Work package: R1
Sub-lot: R1.1

## Implementation

The canonical TCC chapter paths were normalized without rewriting chapter bodies. Existing chapter blobs were reused under their new semantic paths, and `template/main.tex` was changed to load the seven numbered chapters in order.

## First Static result

Static contract run #474 (`34476591229`) failed on head `54cde73b1c0c9c54cc752e6e3f95a752a0107a32`.

The failure was not a LaTeX/content regression. `tests/checks/reference_guide_contract.py` and `standards/reference-guide-map.json` still encoded the previous chapter paths. The contract therefore correctly failed closed when the files were renamed.

Observed stale references included:

- `template/chapters/3-methodology.tex`;
- `template/chapters/related-work.tex`;
- `template/chapters/4-results.tex`;
- `template/chapters/formatting-examples.tex`;
- `template/chapters/5-conclusion.tex`.

The librarian item 11 check also loaded `template/chapters/formatting-examples.tex` directly, so all five expected object titles appeared missing after the rename.

## Correction

The source-to-rule map is updated to the new canonical paths and the librarian checker now uses `template/chapters/6-academic-objects.tex`.

No normative rule IDs, source IDs, classifications, markers, object titles, section labels, thresholds or acceptance semantics are changed. This correction only follows the R1.1 path rename through the existing test/documentation dependency graph.

## R1.1 close condition

R1.1 remains open until a fresh Static contract run passes on the corrected head. The first failed run remains part of the audit trail and must not be hidden or overwritten in the machine state.
