# v3.0.1 R5 Evidence — exact-SHA release regression and candidate transport

Updated: 2026-09-11
Status: REOPENED_CANONICAL_REVIEW
Entry: R4 accepted on `170fec009cc89ec0ca98d8d627cd4c8fb3e8447b`

## Purpose

Bind the complete release matrix to one final canonical `main` SHA without creating a documentation commit that changes the very SHA being certified.

R5 is intentionally a dynamic transport state. Every session resolves current Git/PR/workflow facts first and follows the applicable branch below.

## Single candidate marker

The only R5 marker is:

`release/v3-release-candidate.json`

Do not create `release/final-certification-candidate.json`. Historical references to that absent path are fail-closed test coverage for unknown/alternate marker behavior, not an instruction to add a second source of candidate state.

Changing `release/v3-release-candidate.json` has two deliberate effects:

1. while the current canonical-review correction PR is open, the marker anywhere in the full PR forces Linux Integration `complete`, preventing a later incremental documentation commit from replacing phase-end regression with a scoped green run;
2. after squash merge to `main`, the changed marker triggers automatic complete Linux Integration on the exact canonical SHA. Static runs on every main push, and Linux Release Check runs on the same technical main push.

## Post-merge transport correction

PR #305 was squash-merged before the maintainer canonical review identified the defects below. The resulting `main` SHA is `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d` and still contains the pre-correction canonical `template/main.tex` (placeholder approval date, six-member example committee with unit lines, enabled/printed remissive index).

The post-review correction was first authored on the old pre-squash branch and reached `ca3e16d415a838ef9cd1eb29f2ca3884d1737d7b`, but that branch diverged from the squash-merged `main` and received no CI for the correction commit. It is evidence/source material only, not the transport branch.

The active transport is therefore a clean branch created from current `main`: `release/v3.0.1-canonical-review-corrections`. It applies the corrected `template/main.tex`, canonical chapters, post-textual runtime fix, reference regressions, marker and control state on top of the already merged R0-R4 baseline. This prevents stale/pre-squash history from contaminating the new PR diff and guarantees that new canonical artifacts are rebuilt from the corrected source.

## Maintainer canonical review correction — 2026-09-11

The marker head `c21ffe45f8c8ce5025ff636e0d10a0e67c72964f` completed Static #496, Linux Integration #417 and Linux Release Check #131 successfully. That green state did **not** close R5 because maintainer visual review identified user-facing defects in the canonical TCC before merge.

Release-blocking findings:

1. approval page still rendered the placeholder `Aprovada em: dia de mês de 2026`; the canonical example now uses `11 de setembro de 2026`;
2. the canonical approval page rendered six committee members and unit/department lines; the example must show only three members (advisor + two examiners), each with name and institution only;
3. unnumbered post-textual TOC entries used an empty `\toclabelbox{}`, yielding a visible isolated punctuation mark before dotted leaders for Referências/Glossário and for the optional index when printed; the runtime source is corrected rather than hiding it in the example;
4. the canonical TCC does not need a printed remissive index. `index=imakeidx`, canonical `\index{...}` markers and `\ufcPrintIndex` are removed from the canonical example, while the guide keeps explicit instructions for users who choose to enable the optional resource.

Consequences:
- the green `c21ffe45...` R5 evidence is retained as historical pre-correction evidence only;
- Stage A is reopened and must run again on the correction HEAD;
- because runtime backmatter/bibliography code and publication bytes change, distribution and Release Check/current CTAN `pkgcheck` must be regenerated;
- the corrected canonical PDF and CTAN archive must be made available for maintainer inspection before merge.

## Stage A development run — PR #310 head `b39f48dbc5fef877945e0df7b830f403ddde2c7b`

The clean canonical-review transport initially produced:

- Static Contract #509, run `34646050307`: **PASS**.
- Linux Integration #430, run `34646050352`: **FAIL** in `reference-corpus`.
- Linux Release Check #140, run `34646050326`: **FAIL** on the same `reference-corpus` contract before a release/pkgcheck verdict could close.

The rendered canonical source itself already reflected the intended post-review presentation. The failure was in the regression observer: it compared unnumbered post-textual `REFERÊNCIAS`/ `GLOSSÁRIO` entries against the x-position of numbered chapter titles. The observed values (`REFERÊNCIAS x=85.04`, numbered reference `x=118.04`) are expected after removing the empty numbered-label box from post-textual entries.

Correction: retain the explicit canonical prohibition of `ÍNDICE REMISSIVO` in the TOC, compare numbered chapter titles only against the numbered chapter reference, and compare unnumbered `REFERÊNCIAS` and `GLOSSÁRIO` against each other. This is the previously reviewed contract shape and does **not** restore `\toclabelbox{}`, does not print the optional index, and does not alter runtime semantics.

## Non-closing observer-correction run — `0eff9245c52723a771779244546084c9a84548cf`

The first observer correction produced mixed but useful evidence:

- Static Contract #510, run `34665570308`: **PASS**.
- Linux Integration #431, run `34665570300`: **PASS**, selected `complete`, summary `SCOPE=complete PASS=38 FAIL=0 SKIP=0`; productive Web/Lite E2E PASS. The canonical positive PDF had 62 pages, confirming that the removed default remissive index changed canonical pagination and that README must not freeze the historical 63-page count.
- Linux Release Check #141, run `34665570287`: **FAIL after the complete repository contract and distribution gate passed**. The release run reached `SCOPE=complete PASS=38 FAIL=0 SKIP=0` and distribution PASS, then `release-reference-reproducibility.sh` rejected Unicode extraction because it still required the marker `ÍNDICE`.

That release failure is another stale observer, not a reason to restore the canonical index. The corrected release observer retains positive Unicode markers for institution/resumo/introduction/references and explicitly rejects exact rendered `ÍNDICE REMISSIVO` in the canonical PDF. It does not weaken the optional index fixture/regression elsewhere.

This SHA is non-closing regardless of those results because it also lacks the active final README/site/Pages delivery requirement described below.

## Integrated final-delivery continuity

A maintainer requirement opened before publication requires the final source state to include a comprehensive root README, a project landing page and a pinned GitHub Pages deployment exposing the tracked Web/Lite validator directly.

That delivery surface existed in historical integrated PR #307, but the later clean post-merge PR #310 transport accidentally omitted it. The requirement was never revoked, so `0eff9245...` cannot close Stage A even if its technical gates pass.

PR #310 remains the sole active transport. It absorbs only these reviewed delivery files/contracts from #307:

- `README.md`, adapted to avoid a hard-coded final page count before the corrected TCC is rebuilt;
- `site/index.html`;
- `.github/workflows/pages.yml`;
- delivery/Pages assertions in `tests/checks/validator_source.py`;
- `docs/V3.0.1-R6-EVIDENCE.md`.

No canonical/runtime source is imported from #307. PRs #307/#308/#309 are historical/source material and must not be merged as competing candidates.

Stage A additionally requires Static to emit `readme_delivery=true pages_contract=true`. Stage C additionally requires a successful GitHub Pages deployment from the exact squash-merged `main` SHA and live verification of:

- `https://tiagosombrra.github.io/abntexto-ufc/`;
- `https://tiagosombrra.github.io/abntexto-ufc/validator/`.

If Pages is not enabled for GitHub Actions, enable that external repository setting and rerun deployment on the same source SHA. Do not change certified source to conceal an environment/configuration failure.

## Stage A — pre-merge PR certification

Resolve the current open PR whose head is `release/v3.0.1-canonical-review-corrections` and its exact head. Before merge require on the same head:

- Static Contract PASS, including README/site/Pages delivery contract;
- Linux Integration actually executes `complete`, not a skip or scoped suite;
- complete Linux summary has zero FAIL/SKIP;
- Web/Lite host Chrome E2E executes inside the complete run and passes positive/negative evidence;
- Linux Release Check PASS with complete repository contract;
- current CTAN `pkgcheck` PASS;
- canonical reference reproducibility/PDF-A/font embedding/CLI-Deep evidence PASS;
- seven profile review-pair generation/preflight PASS;
- distribution gate PASS;
- integrated README/site/Pages source present on the same head.

If any check fails, record it in this file/machine state on the correction branch, correct it and rerun before merge.

## Stage B — squash transport to canonical main

Only after Stage A is green, squash-merge the current canonical-review correction PR. PR #305 is historical and already squash-merged to `main` as `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d`; do not try to reopen or reuse it. Repository settings allow squash and disallow merge commits/rebase; use squash explicitly.

Immediately resolve the new `main` SHA from Git. Do not predict or hard-code the squash SHA before merge.

The correction-branch SHA is pre-merge evidence only. The squash SHA becomes the only candidate eligible for final R5 certification.

## Stage C — exact-main certification

The marker/main push must automatically produce:

- Static Contract PASS on exact main SHA;
- Linux Integration `complete` PASS on exact main SHA, including the real Web/Lite Chrome E2E;
- Linux Release Check PASS on exact main SHA;
- current CTAN `pkgcheck` PASS against the exact generated `abntexto-ufc-3.0.1.zip`.

Download the exact-main artifacts and independently verify:

- canonical reference artifact and provenance JSON;
- certified distribution outer digest and `SHA256SUMS`;
- three inner distribution ZIP hashes/inventories;
- CTAN archive shape: one generated project class, zero project `.def`, no full pedagogical reference;
- Template/Overleaf contain the same sanitized full public reference PDF and source;
- no institutional-mark asset/proprietary Microsoft font redistribution;
- seven profile PDF/TEX pairs, manifest and hashes;
- Web/Lite E2E JSON/ChromeDriver evidence;
- canonical full reference PDF visual preflight;
- Pages deployment on the exact main source and live project/validator URL verification.

## No self-invalidating receipt commit

After Stage C exact-main certification succeeds, do **not** commit a source/documentation receipt merely to write the exact SHA or artifact hashes: that would create a new main SHA and invalidate R5.

Until tag/publication freezes the candidate, live exact-main receipts belong to immutable GitHub Actions runs/artifacts and issue #304. The tracked documents already contain this deterministic continuation procedure, so a new session can recover by resolving Git and the issue/run facts.

If a Stage C failure requires a source change, R5 is invalidated. Create/fix through an auditable branch/PR, update these controls there, and repeat Stage A → B → C.

## Transition to R6

R5 is complete only when Stage C passes and the exact-main distribution/review artifacts are independently inspected. Then R6 asks the maintainer to visually approve the full canonical TCC/reference PDF and all seven profile PDF/TEX pairs from that same exact SHA.

No tracked commit or artifact rebuild is allowed between that approval and the `v3.0.1` tag/GitHub Release. CTAN receives exactly the frozen canonical CTAN ZIP.

## Current next action

Let the active runs on `0eff9245...` finish and retain them as non-closing technical evidence. Then advance PR #310 to the integrated delivery successor. The full-PR R5 marker must still force Linux `complete`. Require Static PASS with `readme_delivery=true pages_contract=true`, Linux complete/Web-Lite E2E PASS, Linux Release Check/current `pkgcheck` PASS, and fresh canonical/distribution/review-pair artifacts. Inspect the corrected canonical reference, comprehensive README, landing page and CTAN ZIP. Only that integrated head may close Stage A and be squash-merged.
