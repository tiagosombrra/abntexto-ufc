# v3.0.1 R5 Evidence — exact-SHA release regression and candidate transport

Updated: 2026-09-11
Status: REOPENED_POST_MERGE_CANONICAL_REVIEW
Entry: R4 accepted on `170fec009cc89ec0ca98d8d627cd4c8fb3e8447b`

## Purpose

Bind the complete release matrix to one final canonical `main` SHA without creating a documentation commit that changes the very SHA being certified.

R5 is intentionally a dynamic transport state. Every session resolves current Git/PR/workflow facts first and follows the applicable branch below.

## Post-merge maintainer review — 2026-09-11

PR #305 was squash-merged to `main` as `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d`. Its tree is byte-identical to the pre-merge marker head `c21ffe45f8c8ce5025ff636e0d10a0e67c72964f`.

Before R6/tag/publication, maintainer inspection of the canonical PDF found four release-blocking presentation defects:

1. `Aprovada em: dia de mês de 2026` remained a placeholder;
2. the canonical approval page showed six committee members and unit/department lines instead of a concise three-member example;
3. empty `\toclabelbox{}` prefixes on unnumbered post-textual TOC entries produced a visible isolated punctuation mark before the dotted leader;
4. the canonical PDF printed an unnecessary remissive index.

The correction keeps index support in the public class and guide, but disables/removes index generation from the canonical document. It also fixes the TOC source in project runtime, not by post-processing the PDF.

Because these findings were made after the first squash transport, the exact-main state `ab641d49...` and its green gates are historical evidence only. R5 reopens on branch `release/v3.0.1-post-merge-canonical-review`; PR #307 must repeat pre-merge complete certification and, after squash merge, exact-main certification again.

## Integrated final-delivery surface

A second post-merge requirement was opened after the first exact-main certification: the release must include a complete root README plus a project site exposing the Web/Lite validator directly.

PR #306 (`release/v3.0.1-readme-site-finalization`) implemented and independently passed this delivery-only surface, but it must not be merged separately because PR #307 changes canonical/runtime/publication bytes from the same `main` base. The final candidate therefore absorbs the #306 delivery files into #307:

- `README.md`;
- `site/index.html`;
- pinned `.github/workflows/pages.yml`;
- `tests/checks/validator_source.py` delivery/Pages contract;
- `docs/V3.0.1-R6-EVIDENCE.md`.

Expected post-merge URLs:

- project site: `https://tiagosombrra.github.io/abntexto-ufc/`;
- Web/Lite validator: `https://tiagosombrra.github.io/abntexto-ufc/validator/`.

PR #306 is superseded by the integrated #307 candidate and must not be merged independently.

## First integrated-head failure retained

The initial #307 head `cb9c7098958a602dcbb39a11b8e22ef22303a996` produced Static #500 PASS, but Linux Integration #421 and Linux Release Check #135 failed because `reference-corpus` still compared unnumbered post-textual TOC entries against the x-origin of numbered section titles. The runtime change itself had removed the empty `\toclabelbox{}` as intended.

The corrective gate now keeps two invariants separate:

- numbered top-level titles align with other numbered top-level titles;
- unnumbered post-textual titles begin the TOC line directly and align with each other.

The canonical remissive index remains absent, so it is no longer part of the post-textual alignment loop. No runtime/layout rule is weakened by this test correction.

## Single candidate marker

The only R5 marker is:

`release/v3-release-candidate.json`

Do not create `release/final-certification-candidate.json`. Historical references to that absent path are fail-closed test coverage for unknown/alternate marker behavior, not an instruction to add a second source of candidate state.

Changing `release/v3-release-candidate.json` has two deliberate effects:

1. while active correction PR #307 is open, the marker anywhere in the full PR forces Linux Integration `complete`, preventing a later incremental documentation commit from replacing phase-end regression with a scoped green run;
2. after squash merge to `main`, the changed marker triggers automatic complete Linux Integration on the exact canonical SHA. Static runs on every main push, and Linux Release Check runs on the same technical main push.

## Stage A — pre-merge PR certification

Resolve PR #307 and its exact integrated head. Before merge require on the same head:

- Static Contract PASS;
- Linux Integration actually executes `complete`, not a skip or scoped suite;
- complete Linux summary has zero FAIL/SKIP;
- Web/Lite host Chrome E2E executes inside the complete run and passes positive/negative evidence;
- Linux Release Check PASS with complete repository contract;
- current CTAN `pkgcheck` PASS;
- canonical reference reproducibility/PDF-A/font embedding/CLI-Deep evidence PASS;
- seven profile review-pair generation/preflight PASS;
- distribution gate PASS.

If any check fails, record it in this file/machine state on the correction branch, correct it and rerun before merge.

## Stage B — squash transport to canonical main

Only after Stage A is green, squash-merge PR #307. Repository settings allow squash and disallow merge commits/rebase; use squash explicitly.

Immediately resolve the new `main` SHA from Git. Do not predict or hard-code the squash SHA before merge.

The correction-branch SHA is pre-merge evidence only. The squash SHA becomes the only candidate eligible for final R5 certification.

## Stage C — exact-main certification

The marker/main push must automatically produce:

- Static Contract PASS on exact main SHA;
- Linux Integration `complete` PASS on exact main SHA, including the real Web/Lite Chrome E2E;
- Linux Release Check PASS on exact main SHA;
- current CTAN `pkgcheck` PASS against the exact generated `abntexto-ufc-3.0.1.zip`.

Download the exact-main artifacts and independently verify:

- canonical reference artifact and provenance JSON;
- certified distribution outer digest and `SHA256SUMS`;
- three inner distribution ZIP hashes/inventories;
- CTAN archive shape: one generated project class, zero project `.def`, no full pedagogical reference;
- Template/Overleaf contain the same sanitized full public reference PDF and source;
- no institutional-mark asset/proprietary Microsoft font redistribution;
- seven profile PDF/TEX pairs, manifest and hashes;
- Web/Lite E2E JSON/ChromeDriver evidence;
- canonical full reference PDF visual preflight.

## No self-invalidating receipt commit

After Stage C exact-main certification succeeds, do **not** commit a source/documentation receipt merely to write the exact SHA or artifact hashes: that would create a new main SHA and invalidate R5.

Until tag/publication freezes the candidate, live exact-main receipts belong to immutable GitHub Actions runs/artifacts and issue #304. The tracked documents already contain this deterministic continuation procedure, so a new session can recover by resolving Git and the issue/run facts.

If a Stage C failure requires a source change, R5 is invalidated. Create/fix through an auditable branch/PR, update these controls there, and repeat Stage A → B → C.

## Transition to R6

R5 is complete only when Stage C passes and the exact-main distribution/review artifacts are independently inspected. Then R6 asks the maintainer to visually approve the full canonical TCC/reference PDF and all seven profile PDF/TEX pairs from that same exact SHA.

No tracked commit or artifact rebuild is allowed between that approval and the `v3.0.1` tag/GitHub Release. CTAN receives exactly the frozen canonical CTAN ZIP.

## Current next action

Resolve PR #307 on `release/v3.0.1-post-merge-canonical-review`. Its exact integrated head must contain both the canonical/runtime correction and the absorbed #306 README/site/Pages delivery surface. Require Static PASS, Linux `complete` PASS including Web/Lite E2E, Linux Release Check/current `pkgcheck` PASS, canonical reference/distribution/review-pair gates PASS, and static README/site/Pages contract PASS. Inspect the regenerated canonical PDF specifically for the approval page, TOC and absent canonical remissive index, and inspect README/site/validator delivery files. After exact-head Stage A PASS, close #306 as superseded if still open, squash-merge #307 once, resolve the new exact `main` SHA and repeat Stage C. Stage C additionally requires Pages deployment and live project/`validator/` URL verification. The previous `ab641d49...` evidence remains historical only.
