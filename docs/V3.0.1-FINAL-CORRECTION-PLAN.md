# v3.0.1 final-correction execution plan

Tracking issue: #304
Working branch: `fix/v3.0.1-canonical-tcc-review-findings` (PR #309); merged PR #305 is historical
Historical certified baseline: `111680cd934a4ea55b02f6ffe730ff5260077565`
Target release: `v3.0.1`

## Execution rule

The previous exact-main certification remains historical evidence only. It is no longer the final publication candidate because release-blocking user-facing defects were identified in the canonical TCC/reference distribution and Web/Lite validator surface.

Every lot below must update `release/v3.0.1-final-corrections.json` with status, exact changed files, checks/evidence and unresolved findings. A lot is not DONE merely because CI is green.

No broad repository cleanup is allowed inside this correction cycle unless it directly blocks one of the release criteria below. Whole-repository cleanup remains a separate post-v3.0.1 program.

## R0 — Auditable control-plane reset

Status: DONE.

Acceptance:
- `main` remains unchanged at the historical baseline when the branch is created;
- issue #304 records scope and non-goals;
- machine-readable state records all work packages and blockers;
- publication remains blocked.

## R1 — Canonical TCC source reconstruction

Status: REOPENED. R1.1–R1.3 remain historical accepted work; R1.4 is active after maintainer visual rejection of the prior exact-main candidate. Evidence: `docs/V3.0.1-R1.4-EVIDENCE.md`.

### R1.1 — Source identity normalization

Goal: make source filenames sequential and semantically aligned with the sections they contain, without changing rendered content.

Current -> target mapping:

- `1-introduction.tex` -> `1-introduction.tex`;
- `2-theoretical-framework.tex` -> `2-academic-work-structure.tex`;
- `related-work.tex` -> `3-front-matter.tex`;
- `3-methodology.tex` -> `4-general-formatting.tex`;
- `4-results.tex` -> `5-citations-notes-references.tex`;
- `formatting-examples.tex` -> `6-academic-objects.tex`;
- `5-conclusion.tex` -> `7-abntexto-ufc-resources-and-final-review.tex`.

Acceptance:
- `template/main.tex` loads chapters 1 through 7 in order;
- each filename describes the section title/content;
- section labels and content are preserved in this sub-lot;
- no orphaned legacy chapter files remain.

### R1.2 — Pedagogical coverage inventory

Goal: classify all current reference material against the required teaching contract.

Required coverage dimensions:
- document/profile setup and `\ufcsetup`;
- current normative basis and precedence;
- cover/title/approval/catalog-card policy;
- dedication/acknowledgments/epigraph;
- resumo/abstract/keywords;
- optional lists and table of contents;
- A4, margins, pagination, font, size, color, spacing, paragraph indent;
- section hierarchy;
- indirect, short direct, long direct and apud citations;
- footnotes;
- references/NBR 6023:2025;
- figures, graphs, frames, numerical tables/IBGE, source/note blocks;
- equations;
- source code and algorithms as project-supported academic objects;
- bibliography resources;
- glossary, appendices, annexes, index and conditional spine guidance;
- PDF/A/deposit guidance;
- accessibility and Web/Lite/CLI validation boundaries;
- supported document profiles and portable/strict-font behavior.

Every topic must distinguish one of: `ABNT_NORMATIVE`, `UFC_INSTITUTIONAL`, `PROJECT_POLICY`, `EXAMPLE_OR_RECOMMENDATION`.

### R1.3 — TCC coherent rewrite

Goal: keep the reference document as a real TCC-shaped guide rather than disconnected regression text. Existing reviewer-corrected content should be retained unless the coverage inventory identifies contradiction, duplication or stale guidance.

Acceptance:
- no placeholder research narrative masquerading as a real academic result;
- no normative convenience presented as an ABNT requirement;
- each major rule explains expected appearance, applicable `abntexto-ufc` mechanism and validation/evidence route;
- references used by the guide resolve from `backmatter/references.bib`.

### R1.4 — Maintainer visual-review corrections

Status: IMPLEMENTED_PENDING_CI_AND_VISUAL_REVIEW.

Release-blocking findings:
- use a complete canonical approval date (`11 de setembro de 2026`);
- canonical committee contains exactly advisor + two examiners and no department/center/unit lines;
- remove the artificial standalone period forced immediately after unnumbered/post-textual ToC titles before normal dotted leaders;
- remove the index module, index markers and printed index from the canonical TCC while retaining optional class support in dedicated fixtures.

Acceptance is both automated and visual. The corrected PDF must be rendered and inspected for these exact points before R1.4 can close.

## R2 — Full canonical reference PDF

Status: REVALIDATION REQUIRED. The previous R2 evidence remains historical, but R1.4 changes canonical PDF bytes and therefore requires a fresh deterministic reference artifact before final publication.

Build the full `template/main.tex` document as a first-class release reference.

Acceptance:
- deterministic full PDF generated from exact candidate SHA;
- compile convergence and no unrecognized warning/overflow;
- A4/layout/geometry PASS;
- font embedding PASS;
- PDF/A-2b PASS where claimed;
- CLI/Deep validator PASS;
- hash and source provenance recorded;
- page-by-page visual review completed.

## R3 — Distribution repair

Status: REVALIDATION REQUIRED. The distribution mechanism remains accepted, but R1.4 changes the full public reference source/PDF, so prior Template/Overleaf hashes are historical and new distribution bytes must be regenerated and inspected.

Keep two separate example roles:
- minimal CTAN example: small compile/documentation smoke;
- canonical TCC/reference PDF: complete user-facing teaching/reference document.

Acceptance:
- full reference PDF is generated, not manually edited;
- selected GitHub/template distribution contains the full PDF and its source;
- distribution regression asserts expected presence/hash;
- minimal CTAN example remains independently compilable;
- no institutional marks or proprietary Microsoft fonts are redistributed;
- decision on including the full portable PDF in CTAN is explicit and evidence-based.

## R4 — Web/Lite validator closure

Status: IMPLEMENTATION DONE; FINAL-CANDIDATE RERUN REQUIRED. Web/Lite static/deployment defects remain fixed, but its positive real-PDF E2E must run again against the corrected R1.4/R5 candidate.

Known defect: `validator/app.js` imports `./normative-catalog.js`, but that module is not tracked in the certified `validator/` directory.

Acceptance:
- deterministic browser normative-catalog generation/publication exists;
- clean deploy/package includes every local browser dependency;
- clean-load test catches missing relative imports;
- actual canonical reference PDF runs through the real Web/Lite analysis path;
- a negative PDF produces at least one expected shared failure;
- shared IDs/schema/normative metadata/verdict semantics agree with CLI/Deep;
- deep-only checks remain REVIEW rather than false PASS;
- browser remains local-processing with no upload API.

## R5 — Release-scoped regression

Status: REOPENED_BY_MAINTAINER_VISUAL_FINDINGS. Evidence/transport contract: `docs/V3.0.1-R5-EVIDENCE.md`. The single canonical marker remains `release/v3-release-candidate.json`; the prior exact-main candidate `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d` is superseded.

Required evidence on one final candidate SHA:
- Static contract PASS;
- complete Linux Integration PASS;
- populated unified illustration-list regression PASS;
- Linux Release Check complete PASS;
- current CTAN pkgcheck PASS;
- canonical full reference PDF gate PASS;
- seven profile smoke/review pairs PASS;
- CLI/Deep PASS;
- Web/Lite E2E PASS;
- distribution bundle contents and SHA-256 frozen.

Any source/config/layout/test change after this candidate invalidates R5 and requires recertification.

## R6 — Human acceptance and publication

Status: BLOCKED. The maintainer rejected the prior candidate during visual review; R6 cannot resume until R1.4 and a fresh R5 exact-main certification complete.

Acceptance:
- maintainer reviews the full canonical TCC/reference PDF and all seven profile pairs generated from the exact R5 SHA;
- explicit maintainer approval is recorded externally without a new source commit;
- final tag points to exactly that SHA;
- GitHub Release publishes exactly the R5 bytes;
- re-downloaded release asset hashes match frozen hashes;
- CTAN receives exactly the canonical CTAN ZIP;
- historical `v3.0.0` remains untouched.

## Deferred post-v3.0.1 program

Not part of R0-R6 unless a direct blocker is proven:
- lifecycle classification of the full tracked repository;
- broad branch pruning;
- broad consolidation of historical process documentation;
- unrelated runtime/API refactors;
- deletion of utilities merely because no literal consumer is found.

These remain in the separate post-v3.0.1 regression plan and must be reconciled only after R6.
