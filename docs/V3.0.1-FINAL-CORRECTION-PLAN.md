# v3.0.1 finalization audit and execution plan

Updated: 2026-09-12  
Status: ACTIVE — CONSOLIDATED FINALIZATION  
Tracking issue: #304  
Sole active release PR: #310  
Working branch: `release/v3.0.1-canonical-review-corrections`  
Canonical baseline main: `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d`  
Target: `v3.0.1`

## Objective

Finish 3.0.1 from one auditable candidate without carrying forward partial transports, stale release authorities or documentation that can redirect a future maintainer to an obsolete state.

The previous exact-main candidate remains useful technical evidence but is not publishable. Maintainer review and the 2026-09-12 regression found additional release blockers after that candidate had passed CI.

## Single-transport rule

PR #310 is the only active release transport.

Do not merge PR #299, #307, #308, #309 or #311. Their useful changes are either historical evidence or are explicitly absorbed into #310. No new parallel release PR should be opened unless #310 becomes technically unrecoverable.

## What remains accepted

R0–R4 implementation/evidence remains historical accepted evidence:

- R0: auditable correction control plane;
- R1: canonical TCC source/coverage rewrite;
- R2: deterministic full canonical reference route;
- R3: sanitized public reference and distribution route;
- R4: Web/Lite/CLI validation boundary.

Those results do not waive fresh R5 certification after final candidate changes.

## Findings that reopened finalization

### C1 — Approval-page presentation/runtime

Required final behavior:

- canonical approval date: `12 de setembro de 2026`;
- canonical committee: advisor + examiner 2 + examiner 3 only;
- each signature block renders only name and institution;
- department/center/program unit metadata remains accepted for compatibility but does not render in committee signature blocks;
- advisor/coadvisor role suffixes are not appended to committee member lines.

PR #311 contains the strongest bounded runtime/test correction for this point, but it is a partial divergent transport and failed full CI because `reference-corpus.sh` still expected retired fifth/sixth committee members. Its unique runtime/test improvements are absorbed into #310; #311 itself is superseded.

### C2 — Canonical post-textual presentation

Retain PR #310 corrections:

- unnumbered Referências/Glossário TOC entries do not use an empty label box;
- remissive index remains supported but is not enabled/printed in the canonical TCC;
- reference observers test numbered and unnumbered alignment within the correct semantic class.

### C3 — Final user delivery

The final candidate includes, in the same SHA:

- comprehensive root README;
- `site/index.html`;
- pinned GitHub Pages workflow;
- direct Web/Lite delivery at `/validator/`;
- static delivery assertions;
- R6 README/site/validator acceptance criteria.

### G1/G2 — Control-plane authority

The repository contained current-looking v3.0.0 recovery files while the permanent repository contract required those same stale surfaces.

Correction:

- current authority is the 3.0.1 continuation/plan/machine state;
- `repository_contract.py` requires current 3.0.1 authority files;
- historical v3.0.0 roadmap/handoff are retained only as historical evidence;
- document lifecycle is explicit in `docs/V3.0.1-DOCUMENT-LIFECYCLE.md`.

### G3 — Active stale version identity

`tools/fetch-reference-images.py` must use a version-neutral project User-Agent rather than `abntexto-ufc/3.0.0`.

### G4 — Historical-document ambiguity

Historical phase documents receive a clear superseded/historical banner and point to the current continuation authority. Their original evidence is retained rather than rewritten as if it had never been true.

## Execution lots

### F0 — Consolidate control plane

Status: IMPLEMENTED IN CANDIDATE, PENDING CI.

- make PR #310 the sole active transport;
- add document/tool lifecycle classification;
- add machine-readable global regression receipt;
- update current handoff/plan/R5/R6 state;
- replace stale required control files in permanent repository contract.

Exit: Static Contract recognizes the current authority chain and no current document points to a superseded PR as active.

### F1 — Converge #310 + bounded #311 correction

Status: IMPLEMENTED IN CANDIDATE, PENDING CI.

- import approval-page runtime signature behavior from #311;
- import approval negative regressions from #311;
- keep #310 TOC/index/delivery corrections;
- set canonical date to 2026-09-12;
- align reference-document and reference-corpus observers.

Exit evidence must include:

`REFERENCE-APPROVAL-EVIDENCE status=PASS date=2026-09-12 committee_members=3 member_fields=name,institution unit_fields=0 advisor_role_suffix=0`.

### F2 — Documentation/governance regression

Status: HYGIENE SUCCESSOR IMPLEMENTED, PENDING FRESH CI.

- update active architecture/release/language docs;
- mark historical control/phase documents explicitly historical;
- remove the active 3.0.0 User-Agent residue;
- retain historical facts without allowing them to override current state.
- close repository-relative README links under static validation;
- reconcile validator Pages wording and architecture/history policy;
- classify the global regression JSON in the lifecycle registry;
- exercise coadvisor unit/role suppression in the approval regression.

### F3 — R5 Stage A on exact PR #310 head

Status: PENDING.

Require on one exact head:

- Static Contract PASS, including README/Pages/current-authority assertions;
- Linux Integration `scope=complete` PASS;
- productive Web/Lite real-PDF E2E PASS;
- Linux Release Check PASS;
- current CTAN `pkgcheck` PASS;
- canonical reference reproducibility/PDF-A/font/Unicode PASS;
- distribution bundles and hashes PASS;
- seven profile review pairs generated;
- independent inspection of generated artifacts.

Any source/test/docs/workflow change after this point reopens Stage A.

### F4 — One squash merge + exact-main Stage C

Status: BLOCKED BY F3.

- squash-merge PR #310 exactly once;
- resolve new `main` SHA dynamically;
- require automatic Static + complete Linux + Linux Release Check on that exact SHA;
- verify all retained artifacts and hashes;
- require GitHub Pages deployment on the same source SHA;
- verify project site and `/validator/` live URLs.

No receipt-only source commit is allowed between Stage C and R6.

### F5 — R6 maintainer acceptance

Status: BLOCKED BY F4.

Maintainer reviews from the exact Stage-C SHA:

- complete canonical TCC/reference PDF;
- approval page and TOC/post-textual presentation;
- all seven PDF/TEX profile pairs;
- README;
- deployed landing page;
- deployed Web/Lite validator and local-processing disclosure;
- frozen distribution hashes and canonical CTAN archive.

Any defect reopens R5.

### F6 — Immutable publication

Status: BLOCKED BY F5.

Invariant:

```text
certified source SHA
== visually approved source SHA
== README/site approved source SHA
== tagged v3.0.1 SHA
== source SHA of published release bytes
```

Then:

1. create immutable `v3.0.1` on the approved SHA;
2. publish the already-frozen R5 artifacts without rebuilding;
3. re-download and verify SHA-256;
4. submit exactly `abntexto-ufc-3.0.1.zip` to CTAN;
5. preserve public `v3.0.0` unchanged as historical evidence.

## Explicitly deferred after 3.0.1

Do not expand this release for:

- broad branch deletion;
- deleting manual PowerShell support tools solely for lack of workflow consumers;
- unrelated runtime/API refactoring;
- physical removal/reorganization of historical evidence documents.

Those require a separate post-release lifecycle change.

## Current next action

Run fresh CI on the hygiene-successor PR #310 head. Static must prove README relative-link closure plus existing delivery/Pages contracts; Linux must execute `complete` and pass the strengthened approval/coadvisor regression and productive Web/Lite E2E; Release must pass the complete matrix and current CTAN `pkgcheck`. Treat #515/#436/#146 on `21e371d3...` as non-closing development evidence. If and only if the successor is green, inspect its fresh artifacts and proceed to the one squash merge.
