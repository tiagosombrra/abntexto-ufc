# v3.0.3 distribution correction

Updated: 2026-09-17  
Tracking issue: #328  
Implementation PR: #329

## Scope

This corrective line fixes the user-facing distribution contract discovered after publishing GitHub Release v3.0.2.

The intended policy is:

- CTAN archive: no UFC institutional mark asset; the CTAN example remains `coat-of-arms=false`;
- Template bundle: includes `assets/institutional/ufc-coat-of-arms.png`, keeps `coat-of-arms=true`, and embeds a reference PDF generated from that exact source;
- Overleaf bundle: includes the same institutional asset, keeps `coat-of-arms=true`, and embeds the same reference PDF;
- proprietary Microsoft font files remain excluded from every archive.

## v3.0.2 finding

The v3.0.2 repository source correctly kept `template/main.tex` with `coat-of-arms=true`, but `tools/build-public-bundles.py` sanitized Template and Overleaf output by changing that setting to `false` and excluding `assets/institutional/`. The bundle regression tests enforced that incorrect policy.

The v3.0.2 CTAN archive is correct and remains immutable. The published GitHub v3.0.2 assets also remain immutable historical bytes. The exact CTAN v3.0.2 archive was submitted on 2026-09-17 and may continue processing independently while v3.0.3 development proceeds.

## Corrective implementation

The v3.0.3 correction:

1. stops rewriting `template/main.tex` for Template/Overleaf;
2. includes exactly `assets/institutional/ufc-coat-of-arms.png` in both user bundles;
3. builds `abntexto-ufc-reference.pdf` with `coat-of-arms=true` and the bundled PNG;
4. fails if the institutional asset is missing or invalid;
5. keeps CTAN-specific tests fail-closed against institutional mark redistribution;
6. requires Template/Overleaf tests to find exactly the expected institutional asset and `coat-of-arms=true`;
7. keeps proprietary Microsoft fonts excluded everywhere.

## Validation evidence

PR #329 head `da91d34394f5e5159a3aaa95567ee86741ce27b3` completed:

- Static Contract #585: PASS;
- Linux Integration #497: PASS;
- complete regression: `SCOPE=complete PASS=38 FAIL=0 SKIP=0`;
- distribution evidence: `ctan_institutional_marks_redistributed=false`;
- distribution evidence: `template_overleaf_coat_of_arms=true`;
- Template/Overleaf reference PDF rebuild identity: PASS;
- corrected reference PDF SHA-256 at that development head: `c9756a841eb3e112cc14a0fd07bda75674b969f4bf2277080f0892b0a8123051`.

That SHA is development evidence only. It is not a frozen v3.0.3 publication receipt because the version/release-control transition occurs later in the same PR.

## Active development state

The repository development line now advances to `3.0.3` while v3.0.2 CTAN processing continues independently.

- `Makefile VERSION = 3.0.3`;
- class identity = `v3.0.3`;
- active release marker target = `3.0.3`;
- candidate state = `NOT_FROZEN`;
- candidate SHA = null;
- publication authorization = false;
- tracking issue = #328.

The previous frozen v3.0.2 marker is preserved under `release/history/v3/v3.0.2-release-candidate.json`.

## Release discipline

v3.0.3 development and validation may proceed before CTAN finishes processing v3.0.2. Publication remains ordered:

1. never mutate or replace v3.0.2 tag, GitHub Release assets, certified checksums or CTAN submission bytes;
2. develop and validate v3.0.3 normally with `NOT_FROZEN` active state;
3. freeze v3.0.3 only after complete technical certification and explicit maintainer visual acceptance of the corrected bundles/PDF;
4. create the v3.0.3 tag/Release only from the exact frozen candidate SHA and certified bytes;
5. submit v3.0.3 to CTAN only after CTAN has accepted/published v3.0.2.

Until the v3.0.3 freeze event, no generated 3.0.3 archive is a publication artifact.
