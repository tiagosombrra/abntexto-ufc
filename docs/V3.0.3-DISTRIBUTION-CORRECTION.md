# v3.0.3 distribution correction

Updated: 2026-09-17  
Tracking issue: #328  
Implementation PR: #329 — merged

## Scope

This corrective line fixes the user-facing distribution contract discovered after publishing GitHub Release v3.0.2.

The intended policy is:

- CTAN archive: no UFC institutional mark asset; the CTAN example remains `coat-of-arms=false`;
- Template bundle: includes `assets/institutional/ufc-coat-of-arms.png`, keeps `coat-of-arms=true`, and embeds a reference PDF generated from that exact source;
- Overleaf bundle: includes the same institutional asset, keeps `coat-of-arms=true`, and embeds the same reference PDF;
- proprietary Microsoft font files remain excluded from every archive.

## v3.0.2 finding and immutable boundary

The v3.0.2 repository source correctly kept `template/main.tex` with `coat-of-arms=true`, but the v3.0.2 public-bundle path sanitized Template and Overleaf output by changing that setting to `false` and excluding `assets/institutional/`.

The v3.0.2 CTAN archive is correct for the CTAN surface and remains immutable. The published GitHub v3.0.2 assets also remain immutable historical bytes. The exact certified CTAN v3.0.2 archive was submitted on 2026-09-17 and remains under CTAN processing at the time of this update.

No v3.0.2 tag, Release asset, checksum or CTAN submission byte may be rebuilt or replaced to incorporate the v3.0.3 correction.

## Corrective implementation

The v3.0.3 correction:

1. stops rewriting `template/main.tex` for Template/Overleaf;
2. includes exactly `assets/institutional/ufc-coat-of-arms.png` in both user bundles;
3. builds `abntexto-ufc-reference.pdf` with `coat-of-arms=true` and the bundled PNG;
4. fails if the institutional asset is missing or invalid;
5. keeps CTAN-specific tests fail-closed against institutional mark redistribution;
6. requires Template/Overleaf tests to find exactly the expected institutional asset and `coat-of-arms=true`;
7. keeps proprietary Microsoft fonts excluded everywhere;
8. advances active version/class/release metadata to v3.0.3 while preserving the v3.0.2 publication receipt under `release/history/v3/`.

## Merge receipt

PR #329 was squash-merged into `main` as:

```text
0de4501ed11beccfc2fefff371ecb8fb21e97d07
```

The implementation branch was removed after merge; `main` remained the only remote branch immediately after the merge.

This merge does not freeze or authorize v3.0.3 publication. The active marker remains `NOT_FROZEN`.

## Exact post-merge technical certification

The merged implementation baseline `0de4501ed11beccfc2fefff371ecb8fb21e97d07` completed all post-merge workflows successfully:

- Static Contract #598: PASS;
- Linux Integration #511: PASS;
- Linux Release Check #184: PASS;
- Linux Release Check run id: `35253966868`;
- complete regression: `SCOPE=complete PASS=38 FAIL=0 SKIP=0`;
- current CTAN `pkgcheck 4.1.0`: PASS;
- canonical reference: two byte-identical clean builds;
- canonical reference SHA-256: `c6dbcbc8cee6e77f8629b10b2178ced6acb2e296566c6016c362140692605877`;
- font embedding: PASS;
- repository PDF validator: PASS;
- PDF/A-2b: PASS;
- Unicode extraction: PASS;
- scientific article PDF/A-2b gate: PASS;
- Template/Overleaf reference rebuild identity: PASS;
- cross-bundle reference identity: PASS;
- distribution checksums and archive integrity: PASS;
- CTAN archive contains no institutional mark: PASS;
- Template/Overleaf coat-of-arms contract: PASS;
- proprietary font redistribution: false;
- seven release-review pairs automated preflight: PASS.

The structured distribution evidence for this SHA records:

```text
ctan_institutional_marks_redistributed=false
template_overleaf_coat_of_arms=true
proprietary_fonts_redistributed=false
```

The review-pair evidence records:

```text
pairs=7
a4=PASS
pdfa_2b=PASS
font_embedding=PASS
warnings_overflow=PASS
maintainer_approval=PENDING
```

Therefore the implementation correction is technically certified on the post-merge baseline, but human visual acceptance is not yet recorded.

## Post-merge workflow artifacts

Linux Release Check run `35253966868` retained:

- validation evidence: `linux-release-validation-35253966868`;
- canonical reference: `abntexto-ufc-v3.0.3-canonical-reference-35253966868`;
- seven profile review pairs: `abntexto-ufc-v3.0.3-review-pairs-35253966868`;
- distribution bundle: `abntexto-ufc-v3.0.3-distribution-35253966868`.

These artifacts are **engineering evidence only** because the active marker is still `NOT_FROZEN`. They must not be published as v3.0.3 release assets merely because the workflow is green.

## Active release-control state

The repository development line is now `3.0.3` while v3.0.2 CTAN processing continues independently.

- `Makefile VERSION = 3.0.3`;
- class identity = `v3.0.3`;
- active release marker target = `3.0.3`;
- candidate state = `NOT_FROZEN`;
- candidate SHA = `null`;
- publication authorization = `false`;
- tracking issue = #328;
- authority = this document.

The previous frozen v3.0.2 marker is preserved under `release/history/v3/v3.0.2-release-candidate.json`.

## Why `0de4501...` is not yet the frozen candidate

The post-merge run proves the corrected implementation on `0de4501ed11beccfc2fefff371ecb8fb21e97d07`. This documentation reconciliation is intentionally performed after that merge and therefore creates a newer repository SHA.

The project invariant requires technical certification, visual acceptance and tagging to refer to the same immutable source SHA. For that reason, the final v3.0.3 candidate must be selected only after this documentation update is merged and the resulting final `main` SHA is known.

The implementation-baseline evidence above remains valuable regression evidence, but it does not substitute for the final exact-candidate certification.

## Remaining release sequence

1. merge the documentation reconciliation PR through the normal protected-branch checks;
2. identify the resulting exact `main` SHA;
3. run/confirm Static Contract and Linux Integration on that SHA;
4. run Linux Release Check on that exact SHA and retain the generated canonical PDF, seven review pairs and distribution bundle;
5. require `SCOPE=complete PASS=38 FAIL=0 SKIP=0`, current CTAN `pkgcheck`, reproducibility, PDF/A/font/Unicode gates and the corrected bundle-policy evidence;
6. perform explicit maintainer visual review of the canonical PDF and all seven review pairs generated from the same final SHA;
7. only after technical and visual acceptance, execute the governed v3.0.3 freeze and publication-authorization transition without changing the publication source identity;
8. create annotated tag `v3.0.3` only from the exact frozen candidate SHA;
9. publish the GitHub Release only with the exact certified workflow artifacts and their `SHA256SUMS`;
10. submit the v3.0.3 CTAN archive only after CTAN has accepted/published v3.0.2;
11. record publication receipts in #328 and close it only after the release closeout is complete.

Until the v3.0.3 freeze event, no generated 3.0.3 archive is a publication artifact.

## Publication-order invariant

v3.0.3 development, merge and certification may proceed while CTAN processes v3.0.2. The external CTAN publication order remains strict:

```text
CTAN v3.0.2 accepted/published
    before
CTAN v3.0.3 update submission
```

This ordering does not require rewriting or pausing the v3.0.3 source line; it only constrains the later CTAN upload event.
