# v3.0.1 R3 Evidence — Public distribution repair

Updated: 2026-09-10
Status: IMPLEMENTED_PENDING_CI
Entry source: R2 accepted on `514c128f542b00d4a10a9ad05ce7fc94f770ef55`
Initial R3 implementation source: `e37516470f7374b74f85f4d84d22598830b8b92f`.
Active correction source: resolve the current HEAD of `release/v3.0.1-final-corrections`; the correction must retain the failed run below as development evidence.

## Purpose

R3 repairs the public distribution so users receive both the editable canonical TCC source and a complete compiled reference PDF, while preserving the intentionally small CTAN smoke/example as a separate artifact role.

## Fixed decisions

1. `docs/ctan-example.tex` remains the minimal CTAN compile/documentation example. The full pedagogical TCC is not a replacement for it.
2. The full public reference PDF is named `abntexto-ufc-reference.pdf`.
3. That PDF is generated from the same sanitized public `main.tex` and project runtime used by the template/Overleaf bundles. It is never manually edited and is never copied from the R2 source-tree PDF.
4. Public source sanitization keeps `coat-of-arms=false`; UFC mark assets are not redistributed.
5. Proprietary Microsoft fonts are not redistributed. Portable fallback behavior remains the public build route unless a user supplies licensed fonts locally.
6. R3 extends the existing `tools/build-public-bundles.py` / `tools/build-distribution-bundles.py` pipeline. No parallel release generator is introduced.
7. The CTAN archive remains lean: it keeps the existing minimal example and does not include `abntexto-ufc-reference.pdf`.
8. Template and Overleaf embed the same generated public reference bytes. The gate nevertheless rebuilds each extracted bundle independently and verifies local source-to-embedded-PDF identity before also checking cross-bundle equality.

## Implementation

`tools/build-public-bundles.py` materializes the sanitized public project in a clean temporary directory, injects only the pinned upstream `abntexto.cls` required for the build, exports `SOURCE_DATE_EPOCH`, `FORCE_SOURCE_DATE=1` and `TZ=UTC`, and follows the repository build sequence: pdfLaTeX, conditional Biber, conditional glossary/index processing, then two final pdfLaTeX passes. The final log rejects overfull boxes, unresolved references/citations and unresolved Biber rerun requests. The resulting `main.pdf` is embedded as `abntexto-ufc-reference.pdf` in both public usage bundles.

The editable template ZIP still excludes `abntexto.cls`; the Overleaf ZIP still includes the pinned upstream. The pinned upstream is a release-build input for the template reference generation/rebuild proof, not a newly vendored template dependency.

`tests/integration/distribution-bundles.sh` fails closed unless:

- all public archives remain safe, deterministic and free from proprietary Microsoft font files and institutional mark assets;
- CTAN retains its exact lean shape and does not contain the full pedagogical reference;
- template and Overleaf each contain exactly one `main.tex` and one `abntexto-ufc-reference.pdf`;
- each distributed `main.tex` contains exactly one `coat-of-arms = false` setup and no enabled mark setup;
- each embedded reference starts as a non-empty PDF;
- both public bundles are extracted and rebuilt under the same deterministic release epoch;
- the SHA-256 of each rebuilt `main.pdf` equals the SHA-256 of its embedded `abntexto-ufc-reference.pdf`;
- template and Overleaf embedded reference PDFs are byte-identical;
- the existing outer `SHA256SUMS` and CTAN monolithic-class invariants remain valid.

The gate records both reference hashes, rebuild identity, cross-bundle equality and public mark state in `artifacts/final-certification/distribution-bundles.json`.

## PR integration scope

R3 adds the first-class PR suite `distribution`, containing only `distribution-bundles`. Changes to either public/distribution builder or to the distribution gate select that suite. Orchestration changes combined with distribution changes retain `distribution` rather than fail-closing to the whole integration matrix. The release candidate markers still dominate and force `complete`.

The `distribution-bundles` runner check is PR-only because `make release-check` already owns the release-mode execution of the same gate. This prevents duplicate distribution execution inside the release runner while preserving the independent Linux Release Check workflow.

The TeX Live action executes inside a container with the repository mounted from the GitHub runner. Any integration scope that invokes a Git-aware builder must therefore mark `$PWD` as a Git `safe.directory` inside that container. `tests/checks/linux_integration_suites.py` enforces this orchestration requirement so the distribution suite cannot regress to an unsafe-mounted checkout.

## Failed development evidence retained

Initial implementation SHA: `e37516470f7374b74f85f4d84d22598830b8b92f`.

- Static Contract #488, run `34503194586`: **PASS**.
- Linux Integration #409, run `34503194566`, selected scope `distribution`: **FAIL**.
- Failure point: `distribution-bundles` exited before bundle construction with `Distribution bundle generation requires a canonical Git checkout.`
- Root cause: the Linux Integration TeX Live container had not configured the mounted repository as `safe.directory`; `tools/build-public-bundles.py` uses `git ls-files` to constrain the bundle to tracked source, so Git rejected the mounted working tree before any distribution content check ran.
- Corrective action: add `git config --global --add safe.directory "$PWD"` inside the Linux Integration TeX container and enforce that token in the integration-orchestration contract.
- This failure is development evidence and remains part of R3 history even after a corrected run passes.

Linux Release Check #123, run `34503194470`, was still running when this correction was prepared. The official branch must not move while that run remains active.

## Changed surfaces in the initial implementation

Technical:

- `tools/build-public-bundles.py`;
- `tests/integration/distribution-bundles.sh`;
- `tests/run.py`;
- `tests/integration_suites.py`;
- `tests/checks/linux_integration_suites.py`;
- `.github/workflows/linux-integration.yml`.

Documentation/control:

- `README.md`;
- `docs/CTAN-RELEASE.md`;
- `docs/V3.0.1-R3-EVIDENCE.md`;
- `docs/V3-CONTINUATION.md`;
- `release/v3.0.1-final-corrections.json`.

`tools/build-distribution-bundles.py` is intentionally unchanged: it already delegates template/Overleaf construction to `tools/build-public-bundles.py` and therefore inherits the new full-reference behavior without a second implementation path.

## Acceptance checks — pending

R3 must remain IN PROGRESS until exact-HEAD evidence records:

- Static Contract PASS;
- Linux Integration PASS with selected `distribution` scope for the corrected R3 implementation push;
- Linux Release Check complete PASS on the same corrected implementation HEAD;
- current CTAN `pkgcheck` PASS;
- generated template and Overleaf archive inventories containing `abntexto-ufc-reference.pdf`;
- generated CTAN archive inventory still excluding the full reference PDF;
- embedded reference-PDF SHA-256 for each usage bundle;
- successful clean rebuild of each extracted public source with matching embedded hash;
- proof that `coat-of-arms=false` in distributed source and no UFC mark asset/proprietary Microsoft font is present;
- deterministic outer archive checksums PASS;
- download/independent inspection of the exact-SHA distribution artifact.

Targeted R3 checks are development evidence. R5 remains the mandatory complete exact-SHA phase-end release regression.

## Current next action

Do not advance `release/v3.0.1-final-corrections` while Linux Release Check #123 on `e37516470f7374b74f85f4d84d22598830b8b92f` is active. Once it completes, record its conclusion, reconfirm the official branch HEAD, promote the prepared safe-directory correction, and inspect the new Static, Linux Integration and Linux Release Check runs. If the corrected exact HEAD is green, download and independently inspect its distribution artifact before closing R3 and opening R4.
