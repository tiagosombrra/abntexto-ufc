# v3.0.2 Repository Hygiene and Regression Status

Updated: 2026-09-13  
Tracking issue: #313  
Working branch: `chore/v3.0.2-test-asset-lifecycle` while PR is open  
Runtime baseline after TOC correction: `46ef5e146ab40999c6b4779b15732297f85b5153`

## Scope guardrails

- Preserve the existing five-level section hierarchy.
- Do not redesign the table of contents.
- Keep v3.0.1 tag/release/submitted CTAN bytes immutable.
- Separate runtime fixes from repository hygiene.
- Never delete a branch/file/tool solely because its name looks historical.
- Every cleanup action must retain traceability through Git history, PRs, tags, or relocated evidence.

## Progress

| Work package | Status | Result / next action |
| --- | --- | --- |
| P0 — TOC References/Glossary alignment | DONE | PR #314 squash-merged; PR Static + Linux Integration passed; new main `46ef5e1...` |
| P1 — Branch inventory | DONE | 183 refs in hygiene snapshot; full manifest in `V3.0.2-BRANCH-HYGIENE-MANIFEST.md` |
| P2 — Branch pruning classification | DONE | 181 DELETE-CANDIDATE, 0 REVIEW, 2 KEEP after first second-pass promotion |
| P3 — Workflow audit | DONE (classification) | Keep Static Contract, Linux Integration, Linux Release Check and Pages; no workflow qualifies as legacy |
| P4 — GitHub governance | FINDINGS OPEN | `delete_branch_on_merge=false`; main ruleset has PR/squash/linear-history but no required status checks |
| P5 — Documentation/release-state cleanup | DONE (docs + machine state) | 33 historical documents under `docs/history/v3/`; closed v3/v3.0.1 release JSONs under `release/history/v3/`; active marker reset to v3.0.2 `NOT_FROZEN` |
| P6 — Tool/code/test lifecycle audit | IN PROGRESS (test assets) | Tools classified KEEP; PR #317 proved 168/168 retained test/check scripts reachable; current lot extends the same fail-closed reachability rule to 115 test documents/fixtures |
| P7 — Full post-cleanup regression | PENDING | Canonical PDF, profiles, PDF/A, fonts, distribution, CTAN shape, Template/Overleaf, Web/Lite |
| P8 — v3.0.2 release preparation | PENDING | Only after cleanup + full regression + maintainer visual acceptance |

## Workflow audit

The four workflows have separate responsibilities and should remain:

- **Static Contract** — fast repository/source/normative contract on every main PR/push.
- **Linux Integration** — scoped heavy integration on PRs; complete mode when required.
- **Linux Release Check** — release-grade reproducibility, PDF/A, distributions, current CTAN pkgcheck and human-review artifacts.
- **Pages** — project site and Web/Lite validator delivery.

The sole active release marker is `release/v3-release-candidate.json`. The obsolete `release/final-certification-candidate.json` guard has been removed. Closed v3/v3.0.1 machine state lives under `release/history/v3/` and cannot act as a candidate marker.

## Governance findings

Current repository policy is already squash-only:

- merge commits disabled;
- rebase merge disabled;
- squash merge enabled;
- `main` deletion/non-fast-forward blocked;
- linear history required;
- PR required with thread resolution.

Two improvements remain:

1. enable automatic deletion of merged branches;
2. add appropriate required status checks to the `main` ruleset after deciding which checks are permanent blockers.

These are repository-administration changes and must be applied only through a supported admin-capable operation; source commits cannot substitute for repository settings.

## Release-state lifecycle

The release-state root is intentionally small:

- `release/v3-release-candidate.json` is the only active candidate marker;
- the marker currently represents v3.0.2 development with no frozen candidate;
- closed v3/v3.0.1 machine state is preserved under `release/history/v3/`;
- changes under the historical release namespace are documentation/evidence maintenance, not release-candidate activation.

The current logical branch state is **183 DELETE-CANDIDATE / 0 REVIEW / 2 KEEP** out of 185 remote branches (`main` plus this working branch).

## Current next action

Complete the test-asset lifecycle gate. If all retained documents/fixtures are reachable from permanent test/control surfaces, do not delete them merely to reduce repository size. After P6 closes, run P7 full post-cleanup regression. Physical branch pruning and repository-admin governance settings remain operationally pending because the connected GitHub toolset has no delete-ref or repository-settings mutation operation.

## Validation history

- PR #317 final head `d152dabb0405ad701556bea9c2004525d9bfc9be`: Static PASS; Linux Integration `SCOPE=complete PASS=38 FAIL=0 SKIP=0`; Linux Release Check `SCOPE=complete PASS=38 FAIL=0 SKIP=0`; canonical reproducibility/PDF-A/font embedding/Unicode PASS; CTAN pkgcheck 4.1.0 PASS. Squash-merged to `main` as `59baa7da7cdffe5c81fa08b3fc771a4b6dccf895`.
- PR #317 Static test-surface evidence: 39 runner gates, 16 static checks, 168/168 retained test/check scripts reachable, 0 orphaned.

- PR #315 initial head `515dcaf0666597180d0eed96a1b98137181f9fc5`: Linux Integration PASS.
- Static Contract failed because the permanent repository contract still prohibited every documentation-history path.
- Corrective action: the contract now permits only `docs/history/v3/`, requires explicit historical-snapshot banners for retained Markdown evidence, and continues to reject any other documentation-history root. This is a fail-closed lifecycle rule, not a blanket archive exemption.


## Document-lifecycle lot

After PR #315 merged to `main` as `d36879a24d57a3c8c4dc0ebc0e4317ff2b14f5fd`, the hygiene branch itself became a deletion candidate. The current logical branch state while this document-lifecycle branch is open is:

- KEEP: `main` + `chore/v3.0.2-document-lifecycle`;
- DELETE-CANDIDATE: 182 historical/merged refs;
- REVIEW: 0.

This lot relocates 33 historical v3/v3.0.1 documents from the active `docs/` root into controlled `docs/history/v3/` namespaces and updates current policy/machine-state pointers in the same commit. Historical bodies are retained with explicit snapshot banners rather than rewritten as current truth.
