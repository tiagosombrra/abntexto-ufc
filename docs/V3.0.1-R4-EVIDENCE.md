# v3.0.1 R4 Evidence — Web/Lite static package and real-PDF E2E

Updated: 2026-09-10
Status: VERIFICATION_COMMIT_IN_CI
Entry source: R3 accepted on `2556489da23e16495fc51ebf093f9f4704c6d52f`

## Purpose

Close the two release-blocking Web/Lite defects without changing normative predicates, locator policy, tolerance values or the intended CLI/Deep capability boundary.

## Confirmed entry defects

1. `validator/app.js` imports `./normative-catalog.js`, but the tracked `validator/` tree does not contain that module. A clean static copy of the directory therefore has an incomplete local module graph.
2. `tests/checks/normative_cross_surface.py` proves report/verdict semantics using synthetic vectors, but it does not send a real PDF through the browser's productive `analyze(file, profile)` path.

No repository evidence currently proves a public GitHub Pages deployment. R4 therefore certifies a self-contained static tree and browser execution; public hosting/deployment must not be claimed unless separately demonstrated by Git state or GitHub configuration.

## Fixed implementation decisions

- Keep `standards/catalog.json + standards/precedence.json` as normative source of truth.
- Generate and track `validator/normative-catalog.js` using `tools/normative_catalog.py --emit-web`.
- The source gate independently regenerates that module into a temporary path and requires byte-for-byte identity with the tracked browser module.
- The source gate parses relative ES-module imports and requires every referenced local file to exist inside the tracked static tree.
- Do not create a second validator implementation for testing. The same `analyze` function used by the UI is exported/exposed for browser automation.
- Browser E2E must interact with the productive `validator/index.html` file-upload/profile/analyze controls and obtain the productive report.
- Use an actual canonical/reference PDF as the positive input and a valid deliberately non-A4 PDF as the negative input.
- Positive criteria include `pdf.open=PASS` and `layout.a4=PASS`.
- Negative criteria include `layout.a4=FAIL` and top-level verdict `FAIL`.
- `font.embedded` and `pdfa.deep` must remain `MANUAL REVIEW` in Web/Lite.
- Browser code remains local-processing: no upload API.
- Reuse the three permanent workflows. No fourth workflow is added.

## Implementation

The implementation commit adds a tracked generated normative module, strengthens `validator_source.py` to regenerate/byte-compare it and resolve every relative local ES-module import, exports the productive `analyze` function plus a read-only accessor for the last UI report, and adds a browser E2E that drives the actual file/profile/analyze controls through ChromeDriver.

A PR-only `web-lite-positive` runner check snapshots `template/main.pdf` immediately after the existing reference check. This preserves an exact positive input even when later complete-suite tests clean the template tree. The host-side E2E uses that stable snapshot; it is not a second PDF generator.

The first-class `web-lite` suite contains `validator-source` and `web-lite-positive` (which depends on `reference`). Changes in `validator/`, the source gate or the E2E select that suite. The existing Linux workflow runs Chrome on the host only for `web-lite` or `complete`, and uploads the JSON E2E evidence. No fourth workflow is introduced.

The positive browser profile is `portable`, matching the fallback-font route of the canonical/public template. The positive PDF must pass readable-PDF, A4 and horizontal-margin checks and must not receive a top-level FAIL. The negative fixture is a syntactically valid one-page Letter PDF generated by the E2E itself; it must be readable but fail `layout.a4` and receive verdict FAIL.

## Changed surfaces

Technical:
- `validator/normative-catalog.js` (generated);
- `validator/app.js`;
- `tests/checks/validator_source.py`;
- browser E2E integration script;
- `tests/run.py`;
- `tests/integration_suites.py`;
- `tests/checks/linux_integration_suites.py`;
- existing Linux workflow orchestration as required to run the browser E2E outside the TeX container.

Documentation/control:
- `AGENTS.md`;
- `docs/V3-CONTINUATION.md`;
- this file;
- `release/v3.0.1-final-corrections.json`;
- user-facing validator documentation if implementation creates or changes an invocation path.

## Failed development run retained

First R4 implementation source: `57043432a5e1f94780bfbac3af0b9b199dd12d27`.

- Static Contract #491, run `34532743661`: **PASS**. It proved `relative_imports=1`, generated catalog byte identity, entry module closure, local-processing policy, Web/Lite suite registration and host-browser E2E wiring.
- Linux Integration #412, run `34532743657`: **FAIL only in the new host browser step**. The container-side `web-lite` suite passed `SCOPE=web-lite PASS=3 FAIL=0 SKIP=0`, including the canonical/reference PDF snapshot. GitHub runner provided Google Chrome `152.0.7977.82` and matching ChromeDriver `152.0.7977.82`. The POST to the local ChromeDriver `/session` endpoint exceeded the script's 20-second client timeout before any PDF analysis began.
- No Web/Lite PDF verdict was produced by #412, so the failure is classified as browser-session startup infrastructure/test-harness failure, not evidence that the positive PDF failed validation.

Corrective action: allow up to 90 seconds for cold ChromeDriver session creation, enable verbose ChromeDriver logging, persist `artifacts/validation/web-lite-chromedriver.log`, and upload that diagnostic even if the browser E2E fails. The E2E evidence also records SHA-256 and byte size for both the real positive PDF and generated negative PDF, so later inspection can bind verdicts to exact input bytes. The validator rules, PDF predicates and Deep/Lite boundary are unchanged.

## Second failed development run retained

Corrective source `532a7996613fea0039025d073d00d53e63df707e`:

- Static Contract #492, run `34536446822`: **PASS**.
- Linux Integration #413, run `34536446821`: **FAIL only in host browser startup**. The container-side `web-lite` suite again passed `SCOPE=web-lite PASS=3 FAIL=0 SKIP=0`. Chrome and ChromeDriver remained matched at `152.0.7977.82`. The harness then reported `ChromeDriver did not become ready` after 30 seconds, before any PDF analysis. The intended ChromeDriver log file was not produced, so the diagnostic path itself was not fail-safe.
- Linux Release Check #127, run `34536446905`: **PASS** on the same source, confirming that the browser-harness failure did not regress the release package contract.

Corrective action after #413: the harness creates the diagnostic file before process launch and redirects ChromeDriver stdout/stderr directly into it while retaining `--verbose`. The readiness loop also detects early process exit and includes the retained log tail in the failure. No PDF assertion, normative rule, tolerance or Deep/Lite boundary changes.

## Third failed development run retained

Corrective source `6560a0369e20b953c8ae8321a9e5a21317451735`:

- Static Contract #493, run `34539569313`: **PASS**.
- Linux Integration #414, run `34539569312`: **FAIL before ChromeDriver launch**. The container-side `web-lite` suite again passed `SCOPE=web-lite PASS=3 FAIL=0 SKIP=0`. The host harness then raised `PermissionError: [Errno 13]` while opening `artifacts/validation/web-lite-chromedriver.log`.
- Linux Release Check #128, run `34539569326`: **PASS** on the same source.

Cause: `artifacts/validation` is created/written inside the TeX Live container and remains readable but not host-writable. The later Chrome step runs as the GitHub host user. This is an ownership-boundary defect in the E2E evidence path, not a PDF or validator verdict.

Corrective action after #414: keep the positive PDF as the existing read-only workspace input, but write browser JSON/log only under host-owned `${RUNNER_TEMP}/abntexto-ufc-web-lite`. The workflow creates that directory before E2E and uploads directly from `runner.temp`. No `sudo`, ownership mutation, rebuild, security relaxation or PDF assertion change is introduced.

## Non-closing green successor retained

Source `12e451452749304cd3cc2c35ee73ebba23924001` incorporated the host-owned `${RUNNER_TEMP}` evidence path after #414.

- Static Contract #494, run `34552365712`: **PASS**.
- Linux Integration #415, run `34552366118`: **PASS but non-closing**, because automatic incremental selection chose `smoke`, not `web-lite`; summary `SCOPE=smoke PASS=4 FAIL=0 SKIP=0`. The static Web/Lite source contract still passed, but the host Chrome step was not eligible and no Web/Lite E2E artifact was produced.
- Linux Release Check #129, run `34552365724`: **PASS**.

This green state does not satisfy R4 because the dynamic browser acceptance criterion was not executed. The next commit therefore changes the user-facing `validator/README.md` to document exact manual E2E reproduction. Because `validator/` is a first-class Web/Lite path in `tests/integration_suites.py`, that legitimate documentation change intentionally forces the next incremental PR run to select `web-lite`. No runtime predicate or PDF assertion is changed merely to trigger CI.

## Acceptance — pending CI

R4 is not DONE until one exact implementation HEAD proves:

- Static Contract PASS, including generated catalog byte identity and relative-import closure;
- Linux Integration Web/Lite scope PASS through real Chrome;
- positive canonical/public PDF processed by the productive Web/Lite path;
- valid negative PDF produces the expected shared failure and verdict;
- shared report schema/IDs/normative metadata remain consistent with the existing cross-surface contract;
- `font.embedded` and `pdfa.deep` remain REVIEW in Web/Lite;
- no browser upload/network API is introduced;
- no false claim of public deployment is added;
- failed development runs, if any, remain recorded here before successful reruns.

## Current next action

Inspect Static and Linux Integration on the exact verification HEAD containing the `validator/README.md` manual-E2E documentation change. Require automatic scope selection to be `web-lite`, container summary `PASS=3 FAIL=0 SKIP=0`, host-owned `${RUNNER_TEMP}/abntexto-ufc-web-lite`, ChromeDriver startup, and `WEB-LITE-E2E-EVIDENCE status=PASS`. Independently inspect the uploaded JSON and ChromeDriver log, including exact positive/negative PDF hashes and verdicts. If any gate fails, retain the run/cause before correction and do not weaken PDF assertions. Only this exact-HEAD browser proof may close R4 and open R5.
