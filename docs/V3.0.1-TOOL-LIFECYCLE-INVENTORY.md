# v3.0.1 Tool Lifecycle Inventory

Updated: 2026-09-11
Status: ACTIVE AUDIT INVENTORY
Baseline main: `ab641d49c5f6a79ff54a16a78d1946a6e008fe6d`

## Policy

A tool is not removed solely because a text search finds no caller. Release-critical and manually operated support utilities may be intentionally standalone. This inventory distinguishes executable current consumers from manual/legacy review.

| Tool | Classification | Evidence / consumer | Pre-publication action |
|---|---|---|---|
| `tools/build-distribution-bundles.py` | KEEP — RELEASE CRITICAL | `make distribution-bundles`, Release Check, distribution gate. | Keep. Its `3.0.0` strings are negative sentinels rejecting stale publication text. |
| `tools/build-public-bundles.py` | KEEP — RELEASE CRITICAL | public/distribution bundle build and R3 identity gate. | Keep. |
| `tools/convert-encoding-to-unicode.ps1` | REVIEW — MANUAL/LEGACY | No caller found in current Makefile, permanent workflows or reviewed control docs. | Do not delete for 3.0.1. Post-release: document manual owner/use case or remove in separate lifecycle PR. |
| `tools/convert-unicode-encoding-to-glyphs.ps1` | REVIEW — MANUAL/LEGACY | No caller found in current Makefile, permanent workflows or reviewed control docs. | Same as above. |
| `tools/fetch-abntexto.py` | KEEP — RELEASE CRITICAL | Makefile public/distribution build; pinned upstream `abntexto 1.1` with hash/marker verification. | Keep. Clarify in docs that 1.1 is upstream dependency, not package version. |
| `tools/fetch-reference-images.py` | KEEP — FIX VERSION RESIDUE | `make reference-assets` / distribution path. | **Fix before publication:** User-Agent still says `abntexto-ufc/3.0.0 reference-assets`. |
| `tools/normative_atomic.py` | KEEP — VALIDATION CORE | Static/validator normative contract. | Keep. |
| `tools/normative_catalog.py` | KEEP — VALIDATION CORE | CLI/Deep, generated Web/Lite catalog, static contract. | Keep. |
| `tools/normative_full.py` | KEEP — VALIDATION CORE | Full normative loader/static contract. | Keep. |
| `tools/pdf_measurement.py` | KEEP — VALIDATION CORE | PDF geometry/typography evidence consumers. | Keep. |
| `tools/pdf_vector_measurement.py` | KEEP — VALIDATION CORE | Vector/object geometry evidence. | Keep. |
| `tools/prepare-windows-fonts.ps1` | REVIEW — MANUAL SUPPORT | Windows font certification fixtures/gates exist, but no current workflow/Makefile/control doc calls this preparation script. | Do not delete for 3.0.1. Post-release: document exact Windows certification setup owner or retire script. |
| `tools/validate-ufc-pdf.py` | KEEP — USER/RELEASE VALIDATOR | CLI/Deep validator and release/reference gates. | Keep. |

## Additional version-string audit

- `tools/fetch-reference-images.py`: stale active `3.0.0` User-Agent — real correction.
- `tools/build-distribution-bundles.py`: two `v3.0.0` strings are deliberate forbidden-phrase sentinels and must remain.
- `tools/fetch-abntexto.py`: `abntexto 1.1` is the pinned upstream dependency identity and is correct.
- no other `tools/` match for active `3.0.0`, old recovery branch, TODO or FIXME was found in this pass.

## Windows note

`tests/integration/windows-font-pdfa.sh` certifies prepared Windows PDFs, and `tests/integration/font-poc.sh` can exercise literal Windows fonts, but neither calls `prepare-windows-fonts.ps1`. This confirms that the preparation script is currently a manually operated support surface rather than a permanent automated workflow dependency.
