\pdfinfoomitdate=1
\pdftrailerid{}
\pdfsuppressptexinfo=15
\documentclass[11pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[brazil,english]{babel}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{fancyvrb}
\geometry{margin=2.5cm}
\hypersetup{hidelinks,pdftitle={abntexto-ufc package manual},pdfauthor={Tiago Guimarães Sombra}}
\setlist{nosep}
\newcommand{\pkg}{\texttt{abntexto-ufc}}
\newcommand{\upstream}{\texttt{abntexto}}
\newcommand{\version}{3.0.0}

\title{\pkg\\Package manual}
\author{Tiago Guimarães Sombra}
\date{Version \version}

\begin{document}
\maketitle

\section{Purpose and status}

\pkg{} is an unofficial, community-maintained LaTeX class for academic works at the Federal University of Ceará (UFC), Brazil. It is built on top of the \upstream{} class and provides UFC-oriented front matter, academic-work profiles, bibliography integration, objects, and related formatting support.

The project must not be described as an official or UFC-homologated template unless the University explicitly grants that status.

\section{Requirements}

The class requires LaTeX2e and \upstream{} 1.1 or newer. TeX Live 2026 is the current development target. Bibliography workflows use \texttt{biblatex}/\texttt{biber}; optional class features may load additional packages when enabled.

The package does not redistribute proprietary Microsoft font files or UFC institutional marks. An authorized institutional asset may be supplied locally by the user.

\section{Installation}

When installed through a TeX distribution, start a document with:

\begin{Verbatim}[frame=single]
\documentclass{abntexto-ufc}
\end{Verbatim}

For manual installation, place \texttt{abntexto-ufc.cls} and the accompanying \texttt{abntexto-ufc/} module directory in a location visible to TeX. Install the external \upstream{} dependency separately.

The canonical upstream package page is \url{https://ctan.org/pkg/abntexto}.

\section{Minimal configuration pattern}

Version 3 uses one canonical English project API. Setup keys and project-owned public commands are English; Portuguese remains appropriate for rendered Brazilian academic content. Removed v2 Portuguese project API is not kept through runtime aliases. The full editable template distributed by the project contains the complete set of fields and examples.

\begin{Verbatim}[frame=single]
\documentclass{abntexto-ufc}

\ufcsetup{
  type = undergraduate-capstone,
  print-mode = single-sided,
  author = {Nome Sobrenome},
  title = {Titulo do trabalho},
  location = {Fortaleza},
  year = {2026},
  advisor = {Prof. Dr. Nome do Orientador},
  institution = {Universidade Federal do Ceara},
  undergraduate-program = {Nome do Curso}
}

\begin{document}
\pretextual
\ufcPrintCover
\ufcPrintTitlePage
\ufcPrintTableOfContents

\textual
\section{Introducao}
Texto do trabalho.
\end{document}
\end{Verbatim}

The distribution also contains \texttt{abntexto-ufc-example.tex}, the minimal package example used as a release-source fixture.

\section{Architecture}

The public class file \texttt{abntexto-ufc.cls} loads the external \upstream{} class and then project-owned responsibility modules under \texttt{abntexto-ufc/}. Those modules separate core behavior, fonts, layout, front matter, academic-work profiles, research projects, objects, bibliography, current upstream integrations, standards adaptations, and back matter.

During the v3 runtime/API migration, \texttt{public-api.def} is transitional forwarding debt only; it is not a behavior owner and is removed before the release-ready v3 foundation is frozen. Public behavior is implemented directly by the module that owns it.

This modular layout is intentional: the CTAN candidate keeps the class and module directory together and does not vendor \texttt{abntexto.cls}.

\section{Public distribution variants}

The project produces distinct release artifacts for different consumers:

\begin{itemize}
  \item a class/runtime archive;
  \item a CTAN submission candidate;
  \item a flattened editable template archive;
  \item a flattened Overleaf archive containing the pinned upstream \texttt{abntexto.cls} for self-contained import.
\end{itemize}

Only the Overleaf bundle vendors the pinned upstream class. The CTAN candidate keeps \upstream{} as a normal external TeX dependency.

\section{License and support}

\pkg{} is distributed under the LaTeX Project Public License, version 1.3c or later. The full license text is included in \texttt{LICENSE}.

Repository, source code, issues, and maintenance information are available at:

\begin{center}
\url{https://github.com/tiagosombrra/abntexto-ufc}
\end{center}

\end{document}
