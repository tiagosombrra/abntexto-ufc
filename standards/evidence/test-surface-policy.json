{
  "schema_version": 1,
  "reviewed_at": "2026-09-13",
  "purpose": "Classify retained executable test surfaces, dynamic test assets, and intentionally manual technical control roots while enforcing reachability from permanent repository workflows and runners.",
  "allowed_classes": [
    "release-candidate",
    "platform-staging"
  ],
  "standalone": [
    {
      "path": "tests/checks/public_bundles.py",
      "class": "release-candidate",
      "owner_stage": "release-packaging",
      "purpose": "Validate deterministic public template and Overleaf bundle structure against an explicitly supplied pinned upstream abntexto.cls.",
      "reason_not_permanent_runner": "Requires a separately materialized pinned upstream class and validates distribution staging rather than the ordinary source/runtime contract."
    },
    {
      "path": "tests/checks/distribution_bundles.py",
      "class": "release-candidate",
      "owner_stage": "release-packaging",
      "purpose": "Validate deterministic class, CTAN, template and Overleaf distribution candidates, checksums and package metadata.",
      "reason_not_permanent_runner": "Candidate packaging is a release-stage responsibility and requires an explicitly supplied pinned upstream class."
    },
    {
      "path": "tests/integration/overleaf-stable.sh",
      "class": "platform-staging",
      "owner_stage": "platform-compatibility",
      "purpose": "Compile and inspect a flattened Overleaf-style project with the pinned local upstream class across pdfLaTeX and LuaLaTeX.",
      "reason_not_permanent_runner": "Requires a materialized flattened staging project and release/platform dependencies rather than the canonical repository layout."
    }
  ],
  "dynamic_assets": [
    {
      "path": "tests/documents/arial-font-poc.tex",
      "owner": "tests/integration/font-poc.sh",
      "purpose": "Exercise the Arial branch of the Windows font proof-of-concept matrix.",
      "reason": "The owner selects this document through the shell family loop and the parameterized path tests/documents/${family}-font-poc.tex rather than a literal full-path reference."
    },
    {
      "path": "tests/documents/times-font-poc.tex",
      "owner": "tests/integration/font-poc.sh",
      "purpose": "Exercise the Times New Roman branch of the Windows font proof-of-concept matrix.",
      "reason": "The owner selects this document through the shell family loop and the parameterized path tests/documents/${family}-font-poc.tex rather than a literal full-path reference."
    }
  ],
  "manual_control_roots": [
    {
      "path": "tools/prepare-windows-fonts.ps1",
      "owner_doc": "docs/WINDOWS-FONT-SUPPORT.md",
      "purpose": "Provide the documented Windows preparation entrypoint for local Times New Roman and Arial TeX metrics, maps and font definition files.",
      "reason": "This pipeline is intentionally manual because it depends on locally installed proprietary Microsoft fonts; the entrypoint owns the two encoding-conversion PowerShell helpers transitively."
    }
  ]
}
