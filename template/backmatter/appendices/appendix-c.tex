\label{ap:codigo}

Este apêndice exemplifica código-fonte elaborado pelo autor usando o mesmo ambiente tipográfico empregado no corpo do trabalho. A inclusão de arquivo externo é exercitada separadamente no catálogo de exemplos com Python.

\lstset{language=C++,numbers=left,stepnumber=1,numbersep=8pt}
\legend{code}{Código C++ apresentado como apêndice}
\ufcSource{Elaboração própria.}
\label{cod:apendice-cpp}
\begin{ufclisting}[here]
int soma(int a, int b) {
    return a + b;
}

int main() {
    return soma(2, 3);
}
\end{ufclisting}
\lstset{numbers=none,stepnumber=1}
