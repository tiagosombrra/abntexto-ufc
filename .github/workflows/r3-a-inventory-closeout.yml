name: Temporary R3-A inventory closeout

on:
  push:
    branches:
      - docs/v3-r3-inventory

permissions:
  contents: write

concurrency:
  group: temporary-r3-a-inventory-${{ github.ref }}
  cancel-in-progress: true

jobs:
  closeout:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout inventory branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
      - name: Set up Python 3.13
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.13'
      - name: Set up Node 24
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: '24'
      - name: Reconcile R3-A closeout and R3-B1 entry
        run: |
          git fetch origin main
          python3 .github/scripts/r3-a-inventory-closeout.py
      - name: Validate inventory control plane
        run: |
          set -euo pipefail
          make static-check
          git diff --check
      - name: Remove temporary executor and commit result
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/scripts/r3-a-inventory-closeout.py
          rm -f .github/workflows/r3-a-inventory-closeout.yml
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: close R3-A and activate R3-B1"
          git push origin "HEAD:${GITHUB_REF_NAME}"
